% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixedsimex.R
\name{mixedsimex}
\alias{mixedsimex}
\title{Mixed SIMEX Method for Measurement Error and Misclassification}
\usage{
mixedsimex(
  formula,
  data,
  me.var,
  mc.var,
  me.var.true = NULL,
  mc.var.true = NULL,
  me.matrix,
  mc.matrix,
  lambda = c(0.5, 1, 1.5, 2),
  B = 100,
  fitting.method = "glm",
  family = gaussian(),
  extrapolation = "quadratic",
  ...
)
}
\arguments{
\item{formula}{A formula specifying the model.}

\item{data}{A data frame containing the variables in the model.}

\item{me.var}{Character string specifying the name of the variable with additive measurement error.}

\item{mc.var}{Character string specifying the name of the variable with misclassification.}

\item{me.var.true}{Optional true values for the variable with measurement error (for validation).}

\item{mc.var.true}{Optional true values for the misclassified variable (for validation).}

\item{me.matrix}{Measurement error variance-covariance matrix for the additive error.}

\item{mc.matrix}{Misclassification probability matrix.}

\item{lambda}{A vector of lambda values for the extrapolation step. Default is c(0.5, 1, 1.5, 2).}

\item{B}{Number of simulations for each lambda. Default is 100.}

\item{fitting.method}{Method used for fitting the model. Default is "glm".}

\item{family}{Family object for the model. Default is gaussian().}

\item{extrapolation}{Extrapolation method. Default is "quadratic".}

\item{...}{Additional arguments passed to the fitting function.}
}
\value{
An object of class "mixedsimex" containing the results of the SIMEX procedure.
}
\description{
This function implements the SIMEX method for models with both additive measurement error
and misclassification. It handles situations where one variable is misclassified and
another variable has additive measurement error.
}
\examples{
\dontrun{
# Example with both measurement error and misclassification
data <- data.frame(y = rnorm(100), x_obs = rnorm(100), z_obs = rbinom(100, 1, 0.5))
me.matrix <- 0.5  # Measurement error variance
mc.matrix <- matrix(c(0.9, 0.1, 0.2, 0.8), nrow = 2)  # Misclassification matrix
result <- mixedsimex(y ~ x_obs + z_obs, data = data, 
                    me.var = "x_obs", mc.var = "z_obs",
                    me.matrix = me.matrix, mc.matrix = mc.matrix)
summary(result)
}
}
